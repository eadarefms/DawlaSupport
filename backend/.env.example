# قاعدة البيانات
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dawla_support?schema=public"

# المصادقة
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"

# البريد الإلكتروني (اختياري — إن تُرك EMAIL_HOST فارغًا، تُطبع الرسائل في السجل بدل إرسالها)
EMAIL_PROVIDER="smtp"
EMAIL_HOST=""
EMAIL_PORT="587"
EMAIL_USER=""
EMAIL_PASSWORD=""
EMAIL_FROM="no-reply@aref-marrakech-safi.ma"

# الخادم
PORT="4000"
CORS_ORIGIN="http://localhost:5173"
